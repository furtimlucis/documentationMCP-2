apiVersion: v1
kind: Service
metadata:
  name: {{ include "documentation-reader-mcp.fullname" . }}
  labels:
    {{- include "documentation-reader-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "documentation-reader-mcp.selectorLabels" . | nindent 4 }}
